# مشروع "overly | أوفرلي" — ملف السياق الكامل

> اقرأ هذا الملف أولاً لفهم المشروع بالكامل.

## نظرة عامة

موقع **overly | أوفرلي** لعروض أمازون السعودية بنظام التسويق بالعمولة (Amazon Associates).
كل منتج يُعرض برابط يحمل وسم العمولة، وأي شراء عبره يعطي عمولة لصاحب الموقع.

- **وسم العمولة:** `faraj733-21`
- **الموقع الحي:** https://majeed3575.github.io/majeeddeals/
- **المستودع:** https://github.com/majeed3575/majeeddeals
- **الاستضافة:** GitHub Pages (مجاني)
- **صاحب المشروع:** يفضّل الشرح بالعربية، خلفيته البرمجية بسيطة — الشروحات لازم تكون بخطوات واضحة جداً

## بنية المشروع

```
├── index.html      ← overly Premium: تصميم Apple داكن، Bento، نظرة سريعة، تنقّل جوال وفلاتر منسدلة
├── assets/         ← هوية أوفرلي الداكنة الشفافة + أيقونة التبويب
├── deals.json      ← بيانات المنتجات المعروضة (المصدر الوحيد للعروض)
├── asins.json      ← قائمة متابعة الـ ASIN (تُبذر تلقائياً، يستخدمها Creators API عند التفعيل)
├── aliexpress_products.json ← قائمة روابط/أرقام منتجات AliExpress التي يحدّثها API
├── aliexpress-callback.html ← صفحة الرجوع المسجلة في تطبيق AliExpress
├── scraper.py      ← روبوت بايثون: تحديث حيّ عبر Creators API أو جمع HTML + نشر تيليجرام
├── .github/workflows/scraper.yml  ← تشغيل الروبوت كل 30 دقيقة عبر GitHub Actions
├── browser-extension/   ← إضافة Brave/Chrome لاستخراج بيانات المنتجات
└── android-app/         ← تطبيق أندرويد (WebView) — الكود مكتمل، يبقى ضبط بناء الـ APK (انظر المهام)
```

## كيف تُضاف المنتجات (مهم)

**الوضع الحالي: عبر إضافة Overly Product Studio الجاهزة (الإصدار 5).**

السبب: أمازون تحجب الجمع الآلي (scraping) من سيرفرات GitHub، فالروبوت يرجع فارغاً غالباً.

الطريقة المعتمدة:
1. ثبّت `saudi-deals-product-tool.zip` حسب تعليمات `browser-extension/README.md` (مرة واحدة).
2. افتح صفحة المنتج في Amazon.sa أو AliExpress واضغط أيقونة **Overly Product Studio**.
3. راجع البيانات المستخرجة وعدّل أي حقل ناقص ثم اضغط **إضافة المنتج إلى القائمة**.
4. كرر ذلك مع المنتجات المطلوبة؛ ويمكنك الضغط على **مزامنة الموقع** لجلب المنتجات المنشورة.
5. بعد إعداد مفتاح GitHub محدود الصلاحية مرة واحدة، اضغط **نشر آمن إلى GitHub**. الأداة تدمج
   منتجاتك مع الملف الحالي ولا تمسح المنتجات القديمة. تبقى الطريقة اليدوية متاحة أيضًا.

الأداة تعرض حالة كل منتج (منشور / تعديل محلي / غير منشور / يحتاج مراجعة)، وتستطيع تنزيل
`deals.json` و`asins.json`، وتتحقق من ASIN أو رقم AliExpress والرابط والعنوان والصورة والسعر
قبل الخصم ونسبة الخصم والتصنيف قبل السماح بالنشر.

**الحماية المهمة:** `scraper.py` معدّل بحيث إذا رجع الجمع فارغاً **لا يمسح** `deals.json` اليدوي إطلاقاً (انظر دالة `main()`).

## الحل الدائم: Amazon Creators API (التحديث الحيّ الرسمي)

> ⚠️ **PA-API القديمة أُوقفت نهائياً** (إيقاف رسمي 30 أبريل 2026، إغلاق السيرفر 15 مايو 2026).
> البديل الرسمي هو **Amazon Creators API** — نفس الفكرة (جلب بيانات المنتجات والأسعار برمجياً)
> لكن بمصادقة OAuth 2.0 جديدة. الكود جاهز في دالة `scrape_creators()` وينتظر مفاتيحك فقط.
> عند ضبط المفاتيح يتحوّل السكربت **تلقائياً** للتحديث الحيّ، وإلا يبقى على الوضع اليدوي.

### الشرط المسبق
Amazon تفتح Creators API فقط لحساب أسوشيتس **مقبول نهائياً** حقّق **10 مبيعات مؤهلة خلال آخر
30 يوماً**، ويجب أن يكون المسجِّل **صاحب الحساب الأساسي**.

### 1) الحصول على المفاتيح
1. ادخل Associates Central → قائمة **Tools** → **CreatorsAPI**.
2. اضغط **Create Application** → اكتب اسماً للتطبيق.
3. اضغط **Create Credential** → انسخ واحفظ فوراً (تظهر مرة واحدة فقط):
   - **Credential ID**
   - **Credential Secret**
   - **Version** (للسعودية = **3.2**، منطقة أوروبا)

### 2) إضافة المفاتيح كـ GitHub Secrets (لا تُكتب في الكود إطلاقاً)
GitHub → المستودع → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**:
- `CREATORS_CLIENT_ID` = الـ Credential ID
- `CREATORS_CLIENT_SECRET` = الـ Credential Secret

قيم أخرى لها إعدادات افتراضية جاهزة للسعودية (تُغيَّر فقط عند الحاجة، كـ Secrets إضافية):
`CREATORS_TOKEN_ENDPOINT` (افتراضي `https://api.amazon.co.uk/auth/o2/token`)،
`CREATORS_MARKETPLACE` (افتراضي `www.amazon.sa`)،
`CREATORS_LANGUAGE` (افتراضي `ar_AE`؛ لو ظهر خطأ لغة اجعلها `en_AE`).

### 3) قائمة المتابعة `asins.json`
- ملف بسيط فيه قائمة الـ ASIN التي تريد متابعتها: `["B0DL5FT193", "B08FJ678YK", ...]`.
- أول تشغيلة تبذره تلقائياً من `deals.json` الحالي (لا حاجة لعمل يدوي أول مرة).
- كل تشغيلة يجلب الـ API الأسعار الحيّة لهذه المنتجات ويُظهر **فقط ما عليه خصم فعلي الآن**،
  فتظهر العروض وتختفي طبيعياً حسب توفّرها على أمازون مع بقاء قائمة المتابعة ثابتة.
- لإضافة منتجات لاحقاً: أضِف الـ ASIN إلى `asins.json` على GitHub.

**كيف تُحسب البيانات:** السعر قبل الخصم من `savingBasis` ونسبة الخصم من `savings.percentage`
(كلها بيانات أمازون الرسمية عبر OffersV2) — لا مقارنات مُختلَقة، تطابقاً مع قرارات الالتزام.

## قرارات التزام مهمة (لا تُخالَف)

هذه القرارات اتُّخذت لحماية حساب Amazon Associates من الإغلاق:

1. **لا ادعاء بوجود "كود خصم حصري"** — شاشة التحويل تقول "جاري التحقق من توفر العرض" فقط،
   لأن ادعاء كود غير موجود يُعد Misleading Claim ويعرّض الحساب للإغلاق.
2. **نافذة الخروج تعرض نسبة الخصم الحقيقية الأعلى** من البيانات، لا رقماً ثابتاً مبالغاً.
3. **السعر النهائي** يُخفى ويُستبدل بـ "اضغط لعرض السعر الحالي" — يخدم الـ CTR ويحل مشكلة
   تقادم الأسعار (سياسة أمازون تشترط أسعاراً حديثة ومؤرّخة).
4. **نص الإفصاح** في الفوتر إلزامي بموجب Operating Agreement — لا يُحذف.
5. **رابط الموقع يجب تسجيله** في حساب أسوشيتس (Account Settings → Edit your website list).
6. **أُلغيت ميزة "متوسط السوق المحلي"** بقرار المالك (2026-07-22) — تعذّر التحقق من أسعار
   السوق الحقيقية بشكل موثوق، وأي مقارنة غير متحقَّقة تُعد Misleading Claim وتعرّض الحساب للإغلاق.
   البطاقة تعرض "السعر قبل الخصم · نسبة الخصم" من بيانات المتجر الموثقة فقط.
7. **دعم AliExpress لا يعني اختلاق عروض** — المنصة والفلترة جاهزتان، ولا يظهر منتج AliExpress
   إلا بعد إضافة رابط وصورة وسعر ونسبة خصم موثقة من صفحة المنتج الفعلية.

## مخطط بيانات المنتج

```json
{
  "asin": "B0DL5FT193",
  "store": "amazon",
  "title": "اسم المنتج بالعربي",
  "image": "https://m.media-amazon.com/images/I/...jpg",
  "discount_percent": 32,
  "original_price": 379,
  "category": "الإلكترونيات"
}
```
التصنيفات المسموحة: `الإلكترونيات` / `المنزل` / `الموضة`

منتج AliExpress يستخدم البنية نفسها مع `"store": "aliexpress"`، ويستبدل `asin` بـ
`product_id` ويضيف `url` من نطاق `aliexpress.com` أو `aliexpress.us`. الواجهة تتعرف على
المتجر تلقائياً وتعرض الشارة والرابط المناسبين.

## ربط AliExpress Affiliates API

تم تجهيز الربط الخلفي الآمن. صفحة الرجوع المسجلة في بوابة AliExpress هي:

`https://majeed3575.github.io/majeeddeals/aliexpress-callback.html`

بعد إنشاء التطبيق والموافقة عليه، أضف القيم التالية في GitHub من:
**Settings → Secrets and variables → Actions → New repository secret**:

- `ALIEXPRESS_APP_KEY`
- `ALIEXPRESS_APP_SECRET`
- `ALIEXPRESS_TRACKING_ID`

لا تضع هذه القيم في `index.html` أو `deals.json` أو إضافة المتصفح. عند توفر الأسرار الثلاثة
يُفعّل المسار تلقائياً، ويستخدم بوابة HTTPS الرسمية، ويجلب السعر والخصم بعملة USD المدعومة
ثم يحول السعر إلى الريال بسعر الربط الرسمي 3.75، ويولّد رابط العمولة الرسمي ثم يدمج المنتج
مع عروض Amazon دون حذف العروض القديمة.

### الاكتشاف الآلي وقائمة المتابعة

العمل الآن **آلي وموجّه افتراضياً**: في كل تشغيل يستدعي الروبوت واجهة
`aliexpress.affiliate.product.query` المتاحة للتطبيق عبر ثمانية أبحاث متخصصة، نصفها
للتقنية (USB-C والشحن اللاسلكي والمنزل الذكي وملحقات الكمبيوتر)، والنصف الآخر
لـ **Life Hacks** (التنظيم والمطبخ والتنظيف والسفر). ثم يرتّب المرشحين بدرجة تجمع
المبيعات والتقييم والخصم وارتباط العنوان بالمجال، ويمنع نسخ المنتجات المتشابهة، ويختار
حتى 12 منتجاً بتوازن بين المجالين وقابلاً للشحن للسعودية.

لا يُقبل المنتج إذا كان تقييمه مفقوداً، أو كان خصمه أقل من 20٪، أو لم يحقق الحد الأدنى
للمبيعات، أو لم يرجع له API سعراً ورابط عمولة رسميين. يتم تدوير المنتجات المكتشفة
آلياً وإزالة المنتجات العامة القديمة عندما لا تعود ضمن الاختيار، بينما تبقى المنتجات
التي اختارها المالك يدوياً محفوظة.

إذا أردت إضافة منتجات محددة بجانب الاكتشاف الآلي، أضفها إلى
`aliexpress_products.json` بإحدى الصيغتين:

```json
{
  "products": [
    "https://www.aliexpress.com/item/1005000000000000.html",
    {
      "product_id": "1005000000000001",
      "category": "الإلكترونيات"
    }
  ]
}
```

إذا وجد الروبوت منتجات AliExpress منشورة سابقاً فإنه يبذر قائمة المتابعة منها تلقائياً.
يمكن تعطيل الاكتشاف الآلي عبر `ALIEXPRESS_AUTO_DISCOVERY=false`، أو الرجوع للبحث العام
عبر `ALIEXPRESS_FOCUS_DISCOVERY=false`، وتعديل حد المنتجات
والجودة عبر `ALIEXPRESS_AUTO_LIMIT` و`ALIEXPRESS_AUTO_MIN_DISCOUNT` و
`ALIEXPRESS_AUTO_MIN_VOLUME` و`ALIEXPRESS_AUTO_MIN_RATING`. لا يُنشر أي منتج لا يعيد
له API رابط عمولة رسمي وسعراً وخصماً صالحين.

## تيليجرام (جاهز لكنه غير مفعّل)

`scraper.py` يحتوي كود نشر تلقائي لقناة تيليجرام (عروض خصمها ≥30%، حد 5 منشورات
لكل تشغيلة، منع تكرار 48 ساعة). للتفعيل تُضاف سرّتان في GitHub:
`TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHANNEL_USERNAME`.

## المهام المفتوحة

- [x] تسجيل رابط الموقع في حساب أمازون أسوشيتس
- [ ] إضافة المزيد من المنتجات عبر الإضافة
- [ ] (اختياري) ربط دومين خاص بدل رابط github.io
- [ ] (اختياري) تفعيل قناة تيليجرام
- [ ] (بعد 10 مبيعات مؤهلة خلال 30 يوماً) تفعيل Amazon Creators API — الكود جاهز، تُضاف المفاتيح فقط
