{
  "name": "overly-admin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/admin-page.js && node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
