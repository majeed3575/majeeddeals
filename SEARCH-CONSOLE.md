# ربط أوفرلي مع Google Search Console

الموقع أصبح جاهزاً تقنياً للأرشفة: `robots.txt` و`sitemap.xml` والروابط القانونية وصفحات المنتجات والتصنيفات موجودة. تبقى خطوة إثبات الملكية داخل حساب Google، ولا يمكن وضع رمز تجريبي أو اختلاق رمز تحقق.

## 1) إضافة الموقع

1. افتح <https://search.google.com/search-console> بالحساب الذي سيدير أوفرلي.
2. اختر **إضافة موقع إلكتروني / Add property**.
3. اختر **URL prefix**.
4. أدخل الرابط كاملاً:

   `https://majeed3575.github.io/majeeddeals/`

## 2) إثبات الملكية

الطريقة الأنسب لـ GitHub Pages هي **HTML file**:

1. نزّل ملف التحقق الذي تعطيك Google اسمه، مثل `google1234567890abcdef.html`.
2. ارفع الملف نفسه إلى جذر مستودع `majeeddeals` بجوار `index.html`، من دون تغيير اسمه أو محتواه.
3. افتح رابطه في المتصفح للتأكد، ثم اضغط **Verify** في Search Console.

يمكن استخدام **HTML tag** بدلاً من ذلك، لكن يجب نسخ قيمة Google الحقيقية إلى `<head>` في `index.html`. رمز التحقق علني بطبيعته وليس مفتاح API، ومع ذلك لا تستخدم قيمة افتراضية.

## 3) إرسال خريطة الموقع

بعد نجاح التحقق:

1. افتح **Sitemaps**.
2. أدخل: `sitemap.xml`
3. اضغط **Submit**.

الرابط الكامل هو:

`https://majeed3575.github.io/majeeddeals/sitemap.xml`

يُعاد توليد الخريطة تلقائياً بعد تحديث المنتجات، ويُغيَّر `lastmod` فقط عندما يتغير محتوى الصفحة فعلياً.

## 4) طلب الفهرسة لأول مرة

من أداة **URL Inspection** افحص واطلب الفهرسة بالتدرج:

1. الصفحة الرئيسية.
2. صفحة `categories/`.
3. صفحتان أو ثلاث من أهم التصنيفات.
4. عدد محدود من صفحات المنتجات المهمة.

لا حاجة إلى طلب فهرسة مئات الصفحات يدوياً؛ خريطة الموقع والروابط الداخلية تتكفل بالباقي.

## 5) ما الذي يُراجع أسبوعياً؟

- **Pages / Indexing:** الصفحات المفهرسة وأسباب الاستبعاد.
- **Performance:** الاستعلامات والظهور والنقر ومتوسط المركز.
- **Core Web Vitals:** تجربة الجوال خصوصاً.
- **Enhancements:** أخطاء البيانات المنظمة إن ظهرت.
- **Manual actions / Security issues:** يجب أن تبقى خالية.

ملاحظة: نتائج البحث المباشر من AliExpress لا تُنشئ صفحات قابلة للفهرسة. الفهرسة مخصصة للمنتجات المحفوظة التي مرّت عبر التحديث الآلي وتوجد ضمن `deals.json`.
