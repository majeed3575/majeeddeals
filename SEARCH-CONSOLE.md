# ربط أوفرلي مع Google Search Console

الموقع الأساسي هو `https://overly.live/`. ملفات `robots.txt` و`sitemap.xml` والصفحات القانونية
وصفحات المنتجات والتصنيفات تُبنى آلياً، وتبقى خطوة إثبات ملكية النطاق داخل حساب Google.

## 1) إضافة النطاق

1. افتح <https://search.google.com/search-console> بالحساب الذي سيدير أوفرلي.
2. اختر **إضافة موقع إلكتروني / Add property**.
3. اختر **Domain** واكتب: `overly.live` من دون `https://` أو مسار.
4. ستعطيك Google سجل `TXT`. أضفه في **Cloudflare → overly.live → DNS → Records**.
5. ارجع إلى Search Console واضغط **Verify**. انتشار سجل DNS قد يحتاج بضع دقائق.

ميزة Domain property أنها تغطي `https` والنطاق الأساسي وأي نطاق فرعي مستقبلاً. لا تنشر قيمة
تحقق مختلقة؛ استخدم سجل TXT الحقيقي الذي يعطيك Google فقط.

## 2) إرسال خريطة الموقع

بعد نجاح التحقق:

1. افتح **Sitemaps**.
2. أدخل: `sitemap.xml`
3. اضغط **Submit**.

الرابط الكامل هو:

`https://overly.live/sitemap.xml`

يُعاد توليد الخريطة بعد تحديث المنتجات، ولا يتغير `lastmod` إلا عند تغير محتوى الصفحة.

## 3) طلب الفهرسة لأول مرة

من أداة **URL Inspection** افحص واطلب الفهرسة بالتدرج:

1. `https://overly.live/`
2. `https://overly.live/categories/`
3. أهم صفحتين أو ثلاث من التصنيفات.
4. عدد محدود من صفحات المنتجات المهمة.

لا تطلب فهرسة مئات الصفحات يدوياً؛ خريطة الموقع والروابط الداخلية تتكفل بالباقي.

## 4) ما الذي يُراجع أسبوعياً؟

- **Pages / Indexing:** الصفحات المفهرسة وأسباب الاستبعاد.
- **Performance:** الاستعلامات والظهور والنقر ومتوسط المركز.
- **Core Web Vitals:** تجربة الجوال خصوصاً.
- **Enhancements:** أخطاء البيانات المنظمة إن ظهرت.
- **Manual actions / Security issues:** يجب أن تبقى خالية.

نتائج البحث المباشر من AliExpress لا تُنشئ صفحات قابلة للفهرسة. الفهرسة مخصصة للمنتجات
المحفوظة التي مرّت عبر التحديث الآلي وتوجد ضمن `deals.json`.
